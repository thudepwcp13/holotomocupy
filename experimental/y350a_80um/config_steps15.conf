path=/eagle/APS_IRI/vnikitin/20231007/y350a_80um      # raw data root
path_out=/eagle/APS_IRI/vnikitin/20231007/y350a_80um_rec # output directory
pfile=y350a_80um_4k_08nm                               # scan prefix name

start_step=4          # 1=convert, 2=preprocess, 3=shifts, 4=binned, 5=paganin+fbp
nlevels=4             # number of binning levels (step 4)
start_level_rec=0     # first binning level to reconstruct (0 = coarsest)


nchunk=1              # z-slices per GPU chunk in FBP (step 5)

n=6144                   # 0 = auto-detect from EDF; set >0 to override crop size
nobj=10000                # 0 = auto-compute from n and magnifications; set >0 to override

rotation_center_shift=35.66 # shift of the rotation center with respect to the detector center
paganin=27            # delta/beta ratio for multi-distance Paganin (step 5)
ref_dist=2            # reference distance index for motion-shift baseline (step 3)
log_level=INFO        # DEBUG, INFO, WARNING, ERROR

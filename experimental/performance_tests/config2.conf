# For performance tests modify this
in_file=/data3/vnikitin/syn_sample.h5 # input h5 dataset
path_out=/data3/vnikitin/syn_sample_rec2 # path to save results
nchunk=4 # chunk size to fit to GPU

ntheta=1800 # number of projection angles (max 18000)
nz=2048 # vertical detector size
n=2048 # horionztal detector size
nzobj=3264 # vertical object size
nobj=3264 # horizontal object size (in x and y)
bin=-2 # increasing data sizes 512 -> 512*2^(-bin) 


niter=2 # Number of iterations
checkpoint_step=-1 # save results step (-1 - no save)
error_step=1 # show error step (-1 - no error computation, we typically set 32)

start_theta=0 # angle to start
ndist=4 # number of distances
rotation_center_shift=-2.170113 # shift of the rotation center with respect to the detector center
obj_dtype=float32 # object type, either complex64 or float32 (to save memory)
paganin=20 # Paganin constant to read the initial guess from file
mask=1.1 # Mask radius for tomography
lam_reg=0 # Regularization term weight
lam_prbfit=3.1e-3 # Probe fit term weight
lam_laplacian=0 # 3-D Laplacian regularization weight
rho=1,0.05,0.02 # Scaling for variables for object, probe, and positions
start_iter = 0
log_level=DEBUG # logging level: DEBUG, INFO, WARNING, ERROR






path=/gpfs/jazzy/data/visitor/ls3231/id16a/20250604/Y350a  # raw data root
path_out=/gpfs/jazzy/data/visitor/ls3231/id16a/20250604/Y350a_FT_large_rand_disp_rec # output directory
pfile=Y350a_FT_large_rand_disp_nobin_020nm               # scan prefix name

start_step=5          # 1=convert, 2=preprocess, 3=shifts, 4=binned, 5=paganin+fbp
nlevels=3             # number of binning levels (step 4)
start_level_rec=0     # first binning level to reconstruct (0 = coarsest)

nchunk=4              # z-slices per GPU chunk in FBP (step 5)

n=4096                # 0 = auto-detect from EDF; set >0 to override crop size
nobj=4096             # 0 = auto-compute from n and magnifications; set >0 to override

rotation_center_shift=-11.79  # rotation axis offset from detector centre (pixels)
paganin=60            # delta/beta ratio for multi-distance Paganin (step 5)
ref_dist=0            # reference distance index for motion-shift baseline (step 3)
log_level=INFO        # DEBUG, INFO, WARNING, ERROR

path=/gpfs/jazzy/data/visitor/ls3231/id16a/20240515/Y350c  # raw data root
path_out=/gpfs/jazzy/data/visitor/ls3231/id16a/20240515/Y350c_rec # output directory
pfile=Y350c_HT_015nm                                     # scan prefix name

start_step=1          # 1=convert, 2=preprocess, 3=shifts, 4=binned, 5=paganin+fbp
nlevels=3             # number of binning levels (step 4)
start_level_rec=0     # first binning level to reconstruct (0 = coarsest)

nchunk=4              # z-slices per GPU chunk in FBP (step 5)

n=2048                # 0 = auto-detect from EDF; set >0 to override crop size
nobj=2500             # 0 = auto-compute from n and magnifications; set >0 to override

rotation_center_shift=145.07  # rotation axis offset from detector centre (pixels)
paganin=120           # delta/beta ratio for multi-distance Paganin (step 5)
ref_dist=0            # reference distance index for motion-shift baseline (step 3)
log_level=INFO        # DEBUG, INFO, WARNING, ERROR

path=/gpfs/jazzy/data/visitor/ls3231/id16a/20240515/Y350c_rec
path_out=/gpfs/jazzy/data/visitor/ls3231/id16a/20240515/Y350c_rec_p1  # output directory
in_file=Y350c_HT_015nm.h5                                   # input h5 dataset (relative to path)
# prb_file=nfp_results.h5                                   # NFP probe initial guess (relative to path_out)
#init_vol=                                                   # initial object from external .vol file (absolute path)

nchunk=4              # projections per GPU batch
ntheta=1500           # number of projection angles
start_theta=1         # index of first projection angle
ndist=4               # number of distances

niter=1000            # number of iterations
start_iter=256        # resume from this iteration (0 = start fresh)
checkpoint_step=16    # save checkpoint every N iters (-1 = disabled)
error_step=16         # print error every N iters

nz=2048               # vertical detector size (pixels)
n=2048                # horizontal detector size (pixels)
nzobj=2500            # vertical object size (pixels)
nobj=2500             # horizontal object size in x and y (pixels)
bin=0                 # binning level (0 = full resolution)

rotation_center_shift=145.07  # rotation axis offset from detector centre (pixels)
paganin=120           # delta/beta ratio for Paganin phase retrieval
obj_dtype=complex64   # object array dtype: float32 or complex64
mask=-1               # circular mask radius as fraction of n (-1 = no mask)

lam_prbfit=3.1e-3     # probe fit regularisation weight
lam_laplacian=0       # 3-D Laplacian regularisation weight
rho=1,0.05,0.000001   # gradient step scales: object, probe, positions

pos_checkpoint=checkpoint_0464.h5  # positions checkpoint to load (relative to path)
log_level=INFO
energy=33.35

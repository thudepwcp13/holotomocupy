path=/data3/vnikitin/atomium/20240924/AtomiumS2_new      # raw data root
path_out=/data3/vnikitin/atomium/20240924/AtomiumS2_new_rec # output directory
pfile=AtomiumS2_HT_007nm                                 # scan prefix name

start_step=5          # 1=convert, 2=preprocess, 3=shifts, 4=binned, 5=paganin+fbp
nlevels=4             # number of binning levels (step 4)
start_level_rec=0     # first binning level to reconstruct (0 = coarsest)

nchunk=16             # z-slices per GPU chunk in FBP (step 5)

n=0                   # 0 = auto-detect from EDF; set >0 to override crop size
nobj=2560             # 0 = auto-compute from n and magnifications; set >0 to override

rotation_center_shift=-27.00227  # rotation axis offset from detector centre (pixels)
paganin=120           # delta/beta ratio for multi-distance Paganin (step 5)
ref_dist=0            # reference distance index for motion-shift baseline (step 3)
log_level=INFO        # DEBUG, INFO, WARNING, ERROR
